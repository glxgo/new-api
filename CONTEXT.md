# 产品语境

## 本地实现状态（2026-07-29）

本文件中“具体订阅实例绑定、用户可选接续策略、钱包有限额兜底、续费单线后继、实例备注、批量绑定、订单权益快照、配置归属与实际扣费来源分离”等已确认规则，已经在当前本地工作树实现。实现保持历史 API Key 不迁移；只有用户新建时明确选择，或编辑、批量管理时主动确认，才进入具体订阅实例绑定模式。

当前实现尚未提交、推送或部署。生产环境仍按既有自动订阅分配逻辑运行；正式上线前还需单独执行数据库迁移验证、生产制品构建、蓝绿发布和真实支付/计费验收。

## 订阅套餐

可供用户购买的套餐方案。它描述价格、周期、额度、分组和权益，但不是某个用户实际持有的那一份额度。

同一用户可以多次购买同一个订阅套餐，每次购买得到相互独立的订阅实例。

## 订阅实例

用户通过购买、兑换或管理员发放实际获得的一份订阅权益。每个订阅实例拥有独立的有效期、已用额度、剩余额度和状态，即使来自同一个订阅套餐也不能相互替代。

## 订阅实例备注

用户可以为自己持有的每一份订阅实例单独添加备注，用于区分同一套餐产生的多份实例或不同项目用途。备注不改变公共订阅套餐的名称、价格、额度和权益，也不影响其他用户。

API Key 选择订阅实例、订阅卡片管理、接续预览和归属历史中，应同时展示实例编号、套餐名称和用户备注。

## API Key 订阅绑定

一个 API Key 与一个具体订阅实例之间的额度归属关系。绑定的目的，是让不同项目使用各自指定的订阅额度，而不是仅凭相同分组在多个订阅实例之间自动选择。

未绑定订阅实例的 API Key 保持现有的自动额度选择行为。已绑定的 API Key 由用户为该 Key 选择额度接续策略，平台不替所有用户强制采用同一种失效处理方式。

## 现有 API Key 兼容

功能上线前已经存在的 API Key 保持当前的自动额度选择和扣费行为，不根据历史消费、最近使用的订阅或其他推测自动迁移。

用户可以在编辑现有 API Key 时主动选择归属的订阅实例和额度接续策略。只有用户明确保存该选择后，该 Key 才从现有行为切换到订阅绑定行为。

## 新建 API Key 的额度归属选择

用户存在可用订阅时，新建 API Key 必须显式选择未绑定的自动分配或一个具体订阅实例。平台可以标记推荐项，但不能默认继承自动分配或替用户确认某份订阅。

用户没有可绑定订阅时，应明确说明当前只能使用现有自动分配行为，不能制造已经绑定订阅的假象。

## API Key 创建步骤

API Key 创建流程根据第一步所选分组动态显示。

第一步“基本信息”包含名称、分组、有效期和创建数量。用户选择订阅指定分组时，单独插入第二步“订阅归属”，用户选择系统自动分配或具体订阅实例；选择具体实例后，再展开“该实例用尽后”的立即结束或允许接续设置。原“额度设置”顺延为第三步，“高级设置”顺延为第四步。

第一步所选分组不是订阅指定分组时，不插入订阅归属步骤，继续保持“基本信息、额度设置、高级设置”的三步流程。

钱包兜底上限只在用户选择钱包余额接续后出现。编辑现有 API Key 时保持相同的信息分区，保存前按关键额度关系变更提醒展示差异。

## API Key 分组与订阅兼容

API Key 只能绑定支持该 Key 当前分组的订阅实例。用户修改 API Key 分组后，如果原绑定订阅不再兼容，必须重新选择兼容订阅或改为未绑定的自动分配，不能保留无效关系，也不能由平台静默解除绑定。

不限制分组的通用订阅在 API Key 更换分组后仍可保持绑定。

## 额度接续策略

用户为单个 API Key 选择的订阅失效处理规则。它决定绑定的订阅实例额度不足、到期或失效后，该 Key 是停止调用，还是按用户预先选择的范围寻找后续额度。

额度接续策略必须在创建和编辑 API Key 时保持可见、可修改，不得由平台静默改变。

用户选择具体订阅实例后，第二步先显示“该实例用尽后”的一级选择：“立即结束”或“允许接续”。选择“允许接续”后，再展开具体接续范围，不能再使用“可以调用别的分组”这种容易与 API Key 路由分组混淆的表述。

这里的“其他实例”只表示该用户持有的其他订阅实例，不表示切换 API Key 分组，也不表示切换渠道路由分组。

## 关键额度关系变更提醒

凡是改变 API Key 原有订阅归属、分组、额度接续策略或资金来源，或者让多个 API Key 继承同一选择，都必须向用户明确提醒，不能静默发生。

用户主动操作产生的变更应在保存前展示原值、新值及影响，并由用户确认。系统根据用户预先选择的策略自动执行接续或资金来源切换时，应在变化发生后立即通知用户，并保留变更原因、发生时间和前后关系。

提醒针对状态变化发送，不因同一状态下的每次 API 请求重复触发。

## 额度关系变更提醒渠道

用户主动修改 API Key 的订阅归属、分组、额度接续策略或钱包兜底上限时，保存前使用确认框展示前后差异和影响；确认保存后使用站内提示反馈结果，不额外发送邮件。

系统自动接续订阅、首次切换到钱包余额、钱包兜底达到百分之八十或百分之百，以及没有可用接续来源时，同时发送站内通知和邮件。相同状态只提醒一次，状态发生新的变化后才产生新的提醒。

## 实例用尽后立即结束

指定订阅实例后的处理选项之一。采用该选项的 API Key 只能消费其指定订阅实例的额度。目标订阅实例额度不足、到期、失效或不再符合当前分组时，请求必须停止并明确提示用户，不得自动改扣其他订阅实例或钱包余额。

该选项承接原“严格绑定”的业务含义，但界面不再使用“严格绑定”作为接续策略名称，以便用户直接看见实例用尽后的实际结果。

## 同套餐实例自动接续

允许接续时的范围之一。这里的“同套餐实例”专指用户针对当前订阅实例执行“续费”后，系统通过明确的前序与后继关系创建的下一份订阅实例。

当前实例不可用后，API Key 可以自动切换到这份明确关联的续费后继实例。不得仅凭套餐名称、套餐模板编号或分组相同推测后继关系；没有明确续费关系时，不得把另一份独立购买的同名套餐当作后继实例。

## 续费实例生效时间

续费支付成功后创建新的订阅实例，并以原实例结束时间和续费支付成功时间中较晚的时间作为新实例开始时间。

用户提前续费时，新实例排在原实例结束后生效，不损失原实例剩余天数；用户在原实例已经过期后续费时，新实例从支付成功时间开始，不从历史结束时间倒推。新实例结束时间按续费时所购当前套餐的有效周期从新开始时间计算。

## 续费实例的 API Key 继承

续费支付成功并创建后继订阅实例时，系统一次性复制原实例在该时刻绑定的全部 API Key。后继实例即使尚未开始，也应显示为“待生效”，并允许用户提前查看和管理其绑定关系。

复制完成后，新旧实例的 API Key 绑定分别管理。用户之后在原实例上新增、移除或调整 Key，不得静默同步到后继实例；需要相同变更时，由用户在后继实例管理界面重新批量选择并确认。

继承操作及后续人工调整都必须进入 API Key 归属变更历史，并向用户展示继承来源和发生时间。

## 单线续费链

续费实例使用不可分叉的单线前后关系。每个订阅实例最多拥有一个直接前序实例和一个直接后继实例，只有整条续费链最末端的实例可以再次续费。

已经产生后继实例的旧实例不再重复提供创建续费的操作，而是显示“已续至”及后继实例编号，并允许用户跳转查看。自动接续始终沿明确的后继关系前进。

用户另外购买相同套餐时得到独立订阅实例，不自动并入已有续费链，也不因套餐名称、套餐模板或分组相同形成续费关系。

## 续费所用当前套餐

用户续费时不沿用原订阅实例购买时的历史价格、额度、周期或权益快照，而是按点击续费时该套餐当前可续费版本展示和结算。续费确认页必须把当前价格、额度、周期、适用分组等关键内容展示给用户。

原套餐不可续费但管理员明确配置了续费替代套餐时，续费确认页必须展示原方案与替代方案的差异，用户确认后购买替代套餐，并将新实例接入原单线续费链。

原套餐不可续费且没有明确配置替代套餐时，续费操作不可用，并显示“当前套餐暂不支持续费”。系统不得根据套餐名称、价格、额度或分组相似度自动推测替代套餐。

## 订阅订单权益快照

创建普通订阅购买、原套餐续费或替代套餐续费订单时，系统必须把用户确认的套餐标题、价格、额度、有效周期、周期重置规则、适用分组、并发与速率权益及其他会影响使用或计费的字段完整写入订单快照，不能只保存套餐编号和金额。余额支付和所有外部支付方式使用同一规则。

订单权益快照在对应支付平台订单的有效期内保持不变。支付成功后严格按订单快照创建后继订阅实例，支付回调不得重新读取可能已经变化的套餐配置来替换权益。

支付订单过期后，用户必须重新发起续费，系统重新读取当时的当前套餐并展示确认。后台套餐修改只影响修改后新创建的订单，不追溯修改已经生成的有效待支付订单。

该规则只影响功能上线后新创建的订单。历史已完成订单、历史待支付订单和现有订阅实例不根据当前套餐配置追溯改写。

## 续费引起的 API Key 分组调整

续费所用当前套餐或替代套餐允许的分组发生变化，导致原实例绑定的 API Key 与后继实例不兼容时，不要求用户退出续费逐个修改。

续费确认页必须列出全部受影响 API Key，并明确展示每个 Key 的原分组、续费后的新分组，以及分组变化可能影响的模型和路由范围。未经用户确认不得登记或执行自动分组调整。

用户提前续费时，支付成功只为后继实例和相关 Key 登记“待切换分组”，不立即改变仍在使用原实例的 Key。原实例结束、后继实例开始时，系统原子地完成 Key 分组切换和后继实例绑定；原实例已经过期且新实例支付后立即生效时，则立即执行。

自动分组调整属于用户已确认的关键额度关系变更，必须记录操作者、续费订单、前后分组、计划执行时间和实际发生时间，并在完成后向用户反馈结果。任一 Key 调整失败时，本批“分组切换和后继实例绑定”必须整体失败并告警，不能形成同一批 Key 部分完成而无说明的状态。

## 同分组其他实例自动接续

额度接续策略之一。指定订阅实例不可用时，可以在 API Key 当前分组下其他有效、兼容的订阅实例中选择后续额度，不改扣钱包余额。

## 自动接续后的正式归属

采用同套餐实例自动接续或同分组其他实例自动接续的 API Key，在成功选择后续订阅实例后，新订阅实例成为该 Key 的正式归属。原订阅、新订阅、接续原因和所用策略必须保留为可审计的变更记录，并按关键额度关系变更提醒用户。

钱包余额接续不改变 API Key 的订阅归属，只改变当前资金来源并累计钱包兜底消费。

## 多级接续顺序

用户选择“允许接续”后，可以自行开关同套餐实例自动接续、同分组其他实例自动接续和钱包余额接续，但不能调整三者的执行顺序。

系统固定按“续费产生的明确后继实例、同分组其他可用实例、钱包余额”的顺序寻找额度；全部关闭或全部不可用时停止请求。这样优先使用用户已经购买且明确续费的订阅额度，最后才产生钱包额外扣费。

创建、编辑和 API Key 详情必须展示完整接续链及当前开启状态，不能只显示当前实际使用的一层。

## 自动接续候选

同分组其他实例自动接续存在多个可用订阅实例时，按到期时间最早优先、到期时间相同则订阅编号较小优先的固定规则选择，不维护手动备用订阅优先级列表。

自动接续的当前归属、候选范围、预计下一份订阅和选择规则必须在创建、编辑和 API Key 详情中对用户可见。候选订阅发生变化时，界面应反映当前实际候选；没有候选时也必须明确显示。

## 订阅选择器可用性

新建 API Key 时，只能选择当前有效、分组兼容且仍有可用额度的订阅实例。已到期、已取消或已耗尽的订阅实例可以作为不可用信息展开查看，但不能被选中。

编辑 API Key 时，如果当前绑定订阅已经失效，仍应保留展示其编号、状态、失效原因和“当前绑定”关系，同时禁止再次选择，并提供兼容的新订阅或暂无可用接续订阅的明确结果。

## 配置归属与实际扣费来源

API Key 的配置归属表示用户为该 Key 选择的正式订阅实例；实际扣费来源表示单次请求最终消费的订阅实例或钱包余额。两者必须分别记录和展示，不能用其中一个替代另一个。

API Key 列表和详情应显示配置归属、额度接续策略、预计候选及钱包兜底进度。使用日志和计费详情应显示本次实际扣费来源，并在发生自动订阅接续或钱包余额接续时使用明确标识。

## API Key 归属变更历史

普通用户可以查看自己 API Key 的完整归属变更历史，管理员可以查看全部用户的变更历史。记录应包含时间、操作者、原归属、新归属或钱包资金来源、接续策略、变化原因及钱包兜底上限的前后值。

归属变更历史不因 API Key 解绑、删除或订阅失效而一并删除，以支持后续对账和争议核查。历史记录不得保存或展示完整 API Key 密钥。

## API Key 额度来源状态

API Key 自身的启用、禁用、到期和 Key 额度状态，与其订阅归属或钱包接续状态相互独立。订阅额度来源不可用时，不自动把 API Key 改为禁用或已耗尽。

绑定订阅不可用且无法接续时显示“归属异常”；正在使用钱包余额时显示“钱包兜底中”。用户完成重新绑定、自动接续成功或恢复可用资金来源后，派生状态自动更新，不要求用户再次启用 API Key。

## 已绑定订阅的取消与删除

订阅实例被 API Key 绑定时仍允许管理员取消或作废，但执行前必须显示受影响的 API Key，并确认后续将按各 Key 已选的额度接续策略处理。变化发生后遵循关键额度关系变更提醒。

仍有 API Key 绑定或存在消费、计费、分润及审计关系的订阅实例不得彻底删除，应保留取消或失效记录用于历史对账。只有不存在这些关系的空记录才可以彻底删除。

## 订阅实例共享绑定

一份订阅实例可以同时绑定多个 API Key，是否共享由用户自行决定，不采用平台强制的一对一限制。

共享绑定的所有 API Key 共同消费该订阅实例的额度，同时各自保留自身的有效期、模型、IP 和 Key 额度等访问限制。订阅实例应向用户展示当前绑定的 API Key 数量，帮助用户判断是否继续共享。

## API Key 单一正式归属

一个 API Key 在同一时刻只能拥有一个正式归属订阅实例，不能同时把多个订阅实例作为并列正式归属。其他订阅实例只能通过用户配置的接续链成为候选资金来源。

订阅实例卡片的“管理”允许批量查看和选择兼容的已有 API Key，并为每个 Key 显示当前归属。选择已经归属于其他实例的 Key，表示把该 Key 正式改绑到当前实例；选择原本处于自动分配模式的旧 Key，表示由用户主动将其切换为指定实例模式。

批量提交前必须展示每个 Key 的原实例、新实例及接续策略变化，由用户确认后执行。系统不得把批量选择解释为同一 Key 新增多个并列归属。

用户在订阅实例管理中取消某个 API Key 的绑定后，该 Key 自动回到系统现有的自动额度分配行为，不要求用户再选择去向。提交前必须明确提示该 Key 将不再固定消费当前实例，并可能按系统自动分配规则消费其他可用订阅或钱包；用户确认后才执行。

## 解绑与待生效后继实例

API Key 已经因提前续费被复制到待生效后继实例时，用户从当前实例解绑该 Key，确认框必须分别展示当前绑定和未来计划绑定。

用户可以选择同时取消待生效后继实例中的绑定，也可以只解绑当前实例并保留后继实例生效时的自动绑定。界面默认勾选“同时取消后继绑定”，避免用户认为已经完全解绑后，Key 又在未来被系统绑定回来；最终选择仍由用户决定。

无论选择哪一种，Key 从当前实例解绑后都立即回到系统自动分配。保留的未来绑定必须继续在 Key 详情中以计划状态可见，并显示预计生效时间。

## 批量创建 API Key

用户一次批量创建多个 API Key 时，本批所有 Key 共同采用用户本次选择的订阅实例和额度接续策略，提交前必须明确展示共享关系。

需要让不同 API Key 归属不同订阅实例时，用户应分别创建，或在创建后逐个编辑归属，不在一次批量创建中配置逐 Key 映射。

## 钱包余额接续

额度接续策略之一。采用该策略的 API Key 在指定订阅实例无法继续消费时，改用用户钱包余额维持调用，不自动消费其他订阅实例。

钱包余额接续必须由用户为该 API Key 明确开启，并持续显示其可能产生订阅外支出的含义；钱包余额不足时停止调用。

## 钱包兜底上限

钱包余额接续允许为单个 API Key 产生的最大订阅外支出。启用钱包余额接续时必须设置独立的、有限的钱包兜底上限，不能留空或设为无限。

钱包兜底上限只计算该 API Key 从绑定订阅切换到钱包后产生的消费，不与订阅实例自身额度混算。达到上限后停止调用，即使 API Key 本身设置为无限额度也不能继续扣除钱包余额。

## 钱包兜底绑定周期

钱包兜底消费按 API Key 当前正式归属的订阅实例累计。订阅实例自身的周期额度重置不会清零钱包兜底消费，避免同一归属期间反复产生新的钱包扣费空间。

API Key 正式改绑到另一个订阅实例后进入新的绑定周期，钱包兜底消费重新从零累计。用户主动修改或重置钱包兜底上限时，必须遵循关键额度关系变更提醒。

## 钱包兜底后的订阅恢复

钱包余额是绑定订阅暂时不可用时的备用资金来源。原绑定订阅在周期额度重置后恢复可用时，API Key 自动恢复优先消费该订阅，并按额度关系变更提醒通知用户。

恢复订阅消费不会清零当前绑定周期已经产生的钱包兜底消费。原绑定订阅永久到期或失效时不视为恢复，应继续执行用户选择的其他接续结果。单次请求只使用一种资金来源，不在订阅与钱包之间拆分扣费。

## API Key 独立额度

API Key 自身额度是用户为该 Key 设置的项目级使用上限，与绑定订阅或钱包资金来源的可用额度相互独立。请求必须同时满足 Key 独立额度和实际资金来源额度，实际可用额度取两者中较小值。

API Key 独立额度耗尽时直接停止调用，不触发订阅接续或钱包余额接续。界面应分别展示 Key 独立额度和资金来源额度，不合并为一个含义不清的数字。

## 默认产品视觉系统

所有用户首次使用时看到的统一视觉语言。它将覆盖整个网站，方向是融合 Claude 的品牌气质与用户提供的参考网站风格，而不是单独复刻其中任何一方。

默认产品视觉系统不是一个需要用户主动选择的主题。用户选择其他主题后，个性化主题可以覆盖默认配色，但产品的页面层级、导航关系和交互含义保持一致。

它通过现有的 Default 主题原地升级：没有主动选择主题或仍在使用 Default 的用户自动获得新视觉；已经明确选择其他主题的用户保留原选择，不因升级被重置。

## 融合视觉

默认产品视觉系统的设计方向。Claude 风格与参考网站风格需要形成一套统一语言，不能只在参考布局上替换一组颜色，也不能为了追求 Claude 气质而丢失参考网站的界面秩序。

融合视觉遵循“参考网站决定形，Claude 决定神”：页面骨架、组件形态、信息密度和排版秩序主要吸收参考网站；色彩、字体气质和整体情绪主要吸收 Claude。

融合视觉不复制参考网站的品牌、文案、插画或专属资产。

## 字体层级

默认产品视觉系统使用三层字体关系：页面大标题、内容标题和关键金额使用具有 Claude 气质的衬线字体；正文、导航、按钮、表格和表单使用清晰的无衬线字体；API Key、时间、金额和技术数据使用等宽字体或等宽数字。

字体层级用于同时保留编辑气质、操作效率和数据精确感，不采用全界面衬线字体。

## 明亮色彩层级

默认产品视觉系统采用“米白画布、白色内容层、墨色文字”的明亮层级。米白或奶油白构成页面环境，接近纯白的表面承载卡片、表格和输入内容，墨色深灰仅用于文字、图标和细结构线，不作为大面积背景。

Claude 陶土色用于关键行动与少量品牌强调。成功、警告和错误保留各自的语义色，不统一染成陶土色。

## 明暗模式

默认产品视觉系统保留亮色、暗色和跟随系统三种选择，新用户默认跟随系统，已有用户的明暗偏好不因视觉升级而被覆盖。

亮色模式是“米白画布、白色内容层”的主表达。暗色模式只在系统或用户选择暗色时出现，使用深暖灰画布、略浅内容层和适配后的陶土强调色，不使用生硬纯黑。

## 组件形态

默认产品视觉系统使用克制圆角、细线边框和极少阴影。普通卡片与静态内容不依赖阴影建立层级；弹窗、下拉菜单等真正脱离页面平面的元素才使用轻微阴影。

标签和状态可以使用胶囊形态，普通按钮、输入框和卡片不做过度圆润处理。不使用玻璃拟态、发光效果或装饰性渐变描边。

## 操作层级

每个操作区域原则上只突出一个陶土色实心主操作。普通操作使用白色表面与细边框，低频操作使用文字按钮；删除、封禁等危险操作使用语义红色，不借品牌色表达风险。

操作层级只统一按钮的视觉权重，不改变现有功能、按钮位置或操作流程。

## 表单控件

输入框、选择器和筛选器保留现有排列与交互，统一使用白色表面和暖灰细边框。聚焦状态使用细陶土色描边与轻微外环，错误状态使用红色边框和提示文字。

禁用控件使用暖灰表面，同时保持文字可读，不依赖过低透明度表达不可用状态。

## 数据表格

数据表格保留现有列、分页、固定表头和操作方式。表头使用很浅的暖米白表面与较小的中等字重，表体使用白色表面和细横线分隔，不使用明显斑马纹。

悬停行使用极淡的陶土暖色表面，选中行比悬停状态略明显，使浏览反馈清楚但不干扰高密度数据阅读。

## 浮层与通知

弹窗和抽屉保留现有结构，使用白色表面、暖灰细边框和轻阴影，背景遮罩保持克制，不使用过深的黑色。

短时消息提示使用暖白表面，通过左侧细色条和图标表达成功、警告和错误，不铺满高饱和语义色。危险确认弹窗继续以红色明确标识危险操作。

## 空状态与加载

较大的空页面可以使用原创的细线插画，以墨色线条为主并加入少量陶土色点缀，吸收参考网站的表现方式但不复制其素材。表格空结果和筛选无结果等小型空状态只使用图标与简短文字，不放置大型装饰。

页面初次加载使用暖灰骨架屏；按钮和局部请求使用小型加载指示器。加载反馈按实际更新范围出现，避免局部请求导致整个页面反复闪烁或重播入场效果。

## 既有展示结构

现有页面对卡片、表格、列表和其他内容形态的选择保持不变。视觉升级统一调整颜色、字体、边框、圆角、间距和状态表现，不以风格改版为由把表格重做成卡片、把卡片重做成主从详情，或改变既有交互关系。

## 页面宽度策略

页面宽度由内容类型决定，不以单一固定容器约束全站。API 密钥、渠道、用户、日志和模型等高密度数据页充分利用可用桌面宽度；钱包、个人资料、系统设置和表单详情等阅读或编辑页面使用更收敛的阅读宽度；仪表盘使用随视口变化的网格。

移动端和平板端不继承桌面宽度，而是回落为适合触控和窄屏阅读的单列或数据浏览结构。

## 响应式信息密度

桌面端使用偏紧凑的信息密度，减少卡片、筛选器、表格行和页面区块中过度的空白，使用户可以高效扫描内容。紧凑化只调整尺寸与间距，不改变既有布局、功能或信息关系。

移动端和平板端不机械继承桌面密度，必须保留适合触控的点击区域和清晰的单列阅读节奏。

## 动效基线

默认产品视觉系统保留现有动效体验，不做统一削减。页面进入、侧栏切换、弹窗、下拉菜单、卡片出现、主题切换、加载反馈和少量弹簧反馈继续作为产品体验的一部分。

动效按组件逐项审查：仅弱化局部过强的缩放或无意义的重复播放，并补充侧栏当前位置、筛选展开、复制成功、进度和数值变化等有助于理解状态的反馈。大数据列表的入场动效不应在每次刷新时重放。

所有动效继续尊重系统的减少动态效果偏好。

## 数据可视化色彩

普通业务数据使用 Claude 延展色板，包括陶土色、橄榄色、柔和天蓝、无花果紫和低饱和绿色。同一指标跨页面保持同一颜色，坐标轴和网格线使用低对比度暖灰。

健康度、可用性和告警等状态数据继续使用明确的绿、黄、红语义色，不为了统一品牌气质而弱化状态辨识。面积图可以使用低透明度色块，不使用鲜艳渐变。

数据可视化只调整配色和视觉细节，不改变现有图表类型、数据结构或交互方式。

## 图标与状态语义

产品继续以现有 Lucide 图标为主要图标语言，统一常用尺寸和线宽。少量现存的 Hugeicons 底层组件不为了本轮视觉升级强行重写，后续不再引入第三套图标库。

普通功能图标使用墨灰色，当前导航和品牌主操作可以使用陶土色。健康、可用和成功状态继续使用绿色，降级或待确认使用黄色，故障、不可用和危险操作使用红色；品牌色不能替代业务状态色。

## 页面改版范围

主页和完整认证流程属于后续单独重新制作的入口体验，不在本轮默认视觉升级中改版。完整认证流程包括登录、注册、找回密码、重置密码、验证码和第三方授权登录等连续环节。

公开定价、排行榜、关于、FAQ、教程、协议和错误页保留当前界面结构与视觉布局，只对齐默认产品视觉系统的配色，不套用登录后控制台的页头、密度或页面骨架。

登录后控制台是本轮视觉升级的主要范围，包括普通用户页面和管理员页面。两类页面使用同一套融合视觉，管理员页面继续保留适合复杂管理任务的更高信息密度和现有功能结构，不另设割裂的后台主题。

## 标准页头

登录后页面的内容页头由页面标题、页面操作和正文前的细分隔线组成。标题使用衬线层级，页面主操作位于同一标题区域；默认不显示副标题或说明。

复杂规则、风险提示和使用帮助应靠近相关功能呈现，不为了填充页头而重复解释页面标题。

## 全局顶部栏

登录后产品的全局顶部栏保留现有功能、位置和固定方式，并继续承载品牌、搜索、通知、语言、余额和用户入口。它与米白页面画布自然相连，通过底部细线而不是独立厚重容器区分层级。

搜索、余额、语言和用户入口使用白色小型表面承载，不使用厚重阴影。页面滚动时顶部栏适当提高背景不透明度，确保下方内容不会干扰阅读。

## 主导航侧栏

登录后产品的既有左侧导航。保留当前的信息架构、列表形态、折叠方式和导航交互，不采用“分组纸页 + 当前项便签”的活页本结构。

主导航侧栏使用比页面稍深的暖米白背景。普通菜单保持无边框列表；当前菜单使用白色浅底、左侧细陶土色标记、墨色文字和陶土色图标，不铺整块强调色。分组标题使用低对比度小字，分组通过淡线或留白区分。

折叠后的侧栏保留同一套当前位置语义，不改变为便签式导航。

## 个性化主题

用户主动选择的视觉偏好。它可以调整颜色、字体、圆角等表现属性，不改变功能、信息架构或页面结构。

现有 Anthropic 主题继续作为更纯粹、更偏全界面衬线的 Claude 风格备选，不因 Default 融入 Claude 气质而删除或合并。
