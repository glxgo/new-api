# 外部鹈鹕目标与生产渠道只读核对

核对日期：2026-09-23。以下取代先前仅按地址列出的候选结论。未修改服务器、生产数据库或线上关联。

## 方法与边界

两端服务器分别读取现有配置，在进程内计算带本次随机nonce的SHA256摘要，本地仅内存比对。来源使用只读SQLite事务，在进程内解密后计算；生产使用MySQL只读事务。原始密钥、加密主密钥与摘要不输出、不持久化，也未向供应商提交密钥或发起模型调用。

查询来源8目标及本站支持gpt-6-astra的渠道，地址去尾斜杠及末尾/v1后比对。已确认6项均为同地址、同密钥、同模型，OpenAI格式/type 1、单密钥，无参数/请求头/组织覆盖、系统提示词或代理。此结论确认当前配置关系，不证明每条历史记录当时的凭据状态或实际请求效果相同。

## 已确认的6项

| 外部目标 | 本站渠道 | 当前业务状态 |
| --- | --- | --- |
| ac | #83 ac 020 | 停用 |
| aishenji-958 | #71 Ai神鸡 0.13 | 启用；#74密钥不同 |
| dkl-016-ap-gpt-2hao | #81 dkl 016 | 启用；#80密钥不同 |
| dkl-025 | #80 dkl 025 | 启用；#81密钥不同 |
| opentoken-2469 | #48 opentoken 0.15 | 启用；#49/#76/#85密钥不同 |
| shuxin79 | #90 shuxin 0.12 | 停用 |

## 用户确认入口等价的2项

| 外部目标 | 同密钥对应 | 地址差异 | 排除的同地址候选 |
| --- | --- | --- | --- |
| kedaya | #9 kedaya不降智 0.18 | 外部kedaya.ai，本站sub.kedaya.xyz | #65密钥不同，不能绑定 |
| shuai-gpt020 | #36 帅-pro 0.2 | 外部api.shuaiapi.com，本站oai.sb | #37密钥不同，不能绑定 |

两项模型一致且均为单密钥。匿名只读status查询显示两个帅API域名均返回SHUAI API及sha-ae8cf99，但同产品同版本不足以证明套餐和路由相同；两个可达鸭入口的/api/status均404。没有绕过认证或发起收费探测。

2026-09-23，用户在获知上述域名、渠道编号及差异后明确确认“是一样的”。因此两对入口按对应账号的等价入口处理，关联确定为kedaya→#9、shuai-gpt020→#36。密钥/模型一致来自服务器只读核验，入口等价来自用户确认；不将后者表述成已执行上游路由实测。该确认解决关联身份问题，不构成生产写入或部署授权。

## 当前分组成员

| 渠道 | 当前分组 |
| --- | --- |
| #83 | gpt 稳定分组、gpt会员分组 |
| #71 | gpt 企业专属、gpt会员分组、企业定制套餐分组、套餐专用分组、aihub专用分组、gpt 稳定分组、下游对接专属、gpt pro特惠分组 |
| #81 | 企业定制套餐分组、套餐专用分组、gpt 企业专属、gpt 稳定分组、gpt会员分组 |
| #80 | GPT Pro尊享 |
| #48 | gpt会员分组、企业定制套餐分组、套餐专用分组、gpt 企业专属、aihub专用分组、gpt 稳定分组 |
| #90 | 套餐专用分组、企业定制套餐分组、gpt 稳定分组、gpt 企业专属、aihub专用分组、下游对接专属 |
| #9 | gpt 稳定分组、gpt 企业专属、GPT Pro尊享 |
| #36 | GPT Pro尊享 |

以上仅本次只读时点事实。正式展示实时读取成员和倍率，不将此表写死。本站停用但外部仍测试的渠道按用户最新要求正常展示，前台统一表述，不添加特殊标签；后台说明业务停用，不重新启用渠道。移出分组、删除渠道、撤下模型、缺失能力关系、来源停用/移除及隐藏仍阻止对应展示。

目前8项关联身份均已确认：ac→83、aishenji-958→71、dkl-016-ap-gpt-2hao→81、dkl-025→80、opentoken-2469→48、shuxin79→90、kedaya→9、shuai-gpt020→36。已保存无凭据的本地确认清单 `tools/pelican-archive/confirmed-mappings.json`。

05:03只读快照包含52渠道、717条能力关系。8项关联已接入本地内存验收，32条成员关系的分组名称及倍率全部与快照一致；#83/#90/#36停用状态不变且可参与展示。管理员9个有测试模型的分组，本地default测试账号按现有权限显示其中6个。此为真实配置副本验收，不是生产写入；生产配置与部署写入继续等待用户确认。
