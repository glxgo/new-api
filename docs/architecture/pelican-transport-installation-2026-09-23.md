# 鹈鹕存档受限运输安装记录

2026-09-23，用户明确批准预检查文档A后执行。范围仅为两台服务器的受限存档运输；未部署新版业务应用、迁移业务数据库、保存生产渠道关联、切流或开放页面。

## 实际安装与验收

- 源站新增系统账号pelican-export；root拥有导出脚本、固定命令、专用公钥和sudo规则。账号仅可无参数执行固定导出，未加入Docker组。SSH规则从主配置末尾Include，原配置与sudo配置已备份；sshd语法通过，root最终有效配置前后逐字一致，reload后全新root连接成功。
- 中转站新增系统账号pelican-sync；生成独立身份，固定已核验的源站主机公钥。私钥只留中转站，不进入应用、源码或本地交接文件。
- pull/export实际安装文件SHA256与本地已验证版本一致。安装前本地Python7项与壳脚本语法检查通过，服务器没有运行构建、依赖安装或单元测试。
- systemd配置解析通过。pelican-archive-pull.timer已启用并运行，开机自动启动，每次任务结束约1分钟后再次执行，允许5秒调度精度偏差。它运输已有存档，不触发模型测试。
- 私有状态目录/var/lib/pelican-archive权限0700，archive.json权限0600，属专用拉取账号。普通无关系统账号不可读取；未新增HTTP端口。
- 首次人工启动取得8目标、240记录、最新ID482；随后两次定时器触发均成功。北京时间捕获时点05:45:19、05:47:00、05:48:08向前推进。捕获时间更新只证明重新获取，不能表示模型每分钟重新测试。
- 实际SSH调用验证：id命令、读取文件命令、SFTP均拒绝；PTY和direct TCP forwarding拒绝。专用账号sudo执行其他程序拒绝。SFTP拒绝退出码64；该模式不返回提示文本，不用是否出现提示文本作为判断依据。
- 停timer后旧存档保留。使用仅本次调用的缺失SSH配置模拟拉取失败，返回失败且旧文件字节和inode完全不变；没有改正常连接配置，之后已恢复timer。
- 源站测试容器未重启、无OOM，网站本机HTTP200。中转站业务容器healthy、0重启、无OOM，status成功且版本仍20260921-load-status。未改变源站模型调度和测试配置。

## 当前边界与后续

外部周期变化：export.py每次重新读取全局defaultIntervalMinutes及各目标interval_minutes，导入时更新SourceConfig和目标周期。两套页面的来源周期取全局配置，不能解读为每个目标必定同周期；目标独立配置仍保留。运输约1分钟、本站导入默认5分钟（worker每30秒检查）相互独立，不因源站改为30或120分钟而改写本站后台设置。正常连接且同步启用时，周期变化和完成记录会在后续运输、导入及页面刷新后体现；不是即时推送或准确的下一次开测倒计时。

此刻已实现“外部存档→中转站宿主私有文件”的持续运输。正式业务容器尚未挂载该目录，新表和8项关联未创建；不能称已完成正式页面持续同步。现有本地预览仍使用此前快照，本轮未刷新或重启本地预览。

下一步本地补齐受控初始化工具：建立分组身份、导入真实快照、保存已确认关联、保持页面隐藏；完成验证后再按文档B申请生产迁移和蓝绿候选验收。无需重复申请A账号/定时运输授权。

## 运维与回退

- 中转站查看：systemctl status pelican-archive-pull.timer；journalctl -u pelican-archive-pull.service。日志只记录成功数量或失败类型，不输出完整存档或凭据。
- 暂停运输：systemctl disable --now pelican-archive-pull.timer；检查当前oneshot是否仍在完成。保留旧archive.json，不删除数据。本站后台暂停导入不会停止此timer，也不会停止外部模型测试。
- 撤销源站访问：移走本次专用公钥及sudo规则；如回退SSH Include，先确认期间没有维护者新增修改，再恢复本次备份或精确移除此段，sshd -t通过后reload并验证原管理入口。
- 源站配置备份位于/root/pelican-transport-install.Pb0XXG；中转站本次安装清单位于/root/pelican-transport-install.TS73hS。这些目录不应对外公开。不删除来源数据库、历史记录或既有账号。
- 源站升级如移动grader文件，固定路径导出将失败并保留旧快照；须重新只读核验并更新包装程序。暂无第三方失败告警；可从systemd状态/journal及后续后台存档时间发现过期。
