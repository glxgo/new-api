# 鹈鹕存档本地核验记录

状态：本地开发与真实存档验收；未提交、推送、生产迁移或上线。

## 最新补充：生产预检查与数据库矩阵

本节接续下方本地联调记录；完整证据和分开批准的安装范围见[生产预检查](pelican-production-preflight-2026-09-23.md)。

- GitHub main仍为247b243014f，线上版本及二进制哈希与交接基线一致，无新提交需要合并。本轮没有重做源码可重复构建证明。当前GitHub账号可push，但无admin/maintain权限。
- 本机实际运行SQLite3.50.4、MySQL8.2.0/8.4.11、PostgreSQL16.4，迁移/重复迁移、导入修订、容量回滚、长文本、并发配置更新及历史字段保留测试均通过race验证。这是合成空白库矩阵，240条真实数据端到端仍为下方SQLite验收；未实测MySQL5.7/PostgreSQL9.6。临时实例全部停止，未安装系统服务。
- 新增受限导出与定时运输配置、独立SSH配置参数，Python7项通过；源站sshd经stdin只读解析符合限制。未在服务器安装，尚须实查专用账号登录、两轮运输及原子文件更新的容器可见性。
- 本机已生成Linux应用及迁移预检查制品，未上传，未做Linux候选运行验证。最后model/service/router/preview定向race通过，未改前端。
- 生产尚无新表。首次发布须补受控初始化分组身份、真实快照与8项关联，保持用户页面隐藏，再验收slave候选；初始化工具尚未实现。生产仅换镜像不会替换现有二进制挂载，须使用独立新路径并保留旧制品。

当前下一步为批准后安装受限运输（文档A）；迁移、应用蓝绿发布与开放页面另行批准（文档B）。本轮未进行任何生产写入。

## 当前状态：真实渠道与分组本地联调已通过

此节覆盖下方早期“仍为演示关联”的状态记录。2026-09-23 05:03:05（北京时间）分别只读取得生产拓扑与外部存档：52渠道、717条能力关系，8目标、240条记录。生产拓扑不含密钥、上游地址、用户、订阅或计费行；实际预览仅用本地内存数据库。外部存档SHA256：`353698812cd0cc34f2ce64827a707c4f71502278ee7d08641be246e90900577b`。

- 8项已确认关联加载成功，32条分组成员关系及倍率逐项与快照相等。业务停用的#83/#90/#36仍具备展示资格，没有改变业务状态。管理员9个有测试模型的分组，本地default测试账号按现有可用组权限显示6个；其余业务组仍保留在后台管理目录。
- 修复测试模型目录混入未关联业务模型的问题：仅列有有效存档关联的模型；单目标隐藏和移出分组后撤销模型目录与记录访问。未改业务模型路由。
- 实际HTTP验收读取7条不同精选记录，全部对应目标最新记录；其中5条有可预览原图，接口SVG与来源逐字节一致。4条精选记录跨分组复用相同ID，无复制凑数；无图的最新记录不会被旧好图替换。
- HTTP实际保存并读回目标隐藏、分组别名/描述、标题、逆序排列、自定义7分钟、仅隐藏、暂停及隐藏并暂停；业务渠道字段完全不变。结束后恢复原配置。重复导入0新增。
- Default浏览器从后台保存临时标题，两套前台均读到新标题；已恢复默认文案。两套真实图片详情打开，标准21及原答29如实可查；Default 390px浅色、Classic 390px深浅色的图片比例及页面无横溢实查通过，Default桌面深浅色也已查看。保留三图上/历史下及原有组件，没有引入新UI库。
- `go test -race -vet=off ./cmd/pelican-preview ./service ./router -run 'TestPreview|TestPelican' -count=1`通过；新增覆盖无关联业务模型、隐藏目录、拓扑过期/未来/缺字段/重复/孤立能力拒绝及关联整批回滚。Python6项通过，包括字段白名单拒绝密钥/地址及停用能力保留。Default类型检查、修改正式页面ESLint、双前端生产构建、`go build ./...`与diff检查通过。

本地预览：Default `http://127.0.0.1:4199/`，Classic `http://127.0.0.1:4200/`，后端4198。页面说明拓扑是05:03只读快照，不是实时生产镜像；本地测试账号不包含真实订阅。两次只读运输已结束，本地worker继续按5分钟检查同一私有存档文件，**没有常驻外部拉取进程**，不能宣称持续追新。操作只作用本地内存，重启丢弃本地设置。

下一步发布准备仍有：受限生产运输、MySQL/PostgreSQL实机、届时最新基线复核、蓝绿候选验收、生产关联保存与上线确认。本轮没有生产写入、外部模型调用、推送或上线。

## 最新确认：两对入口等价

用户已明确确认kedaya.ai/sub.kedaya.xyz及api.shuaiapi.com/oai.sb对相应账号等价，关联确定为kedaya→#9、shuai-gpt020→#36。加上先前服务器核验的6项，8个当前目标的关联身份全部确认。入口等价证据为用户确认，未新增上游实测。本轮仅更新文档和交接记录，未改变生产映射或演示预览；实际生产端到端验收、运输及部署门槛仍保留。

## 后续确认：停用渠道正常展示与身份核对

- 用户最新要求取代下文停用即排除的旧规则：本站渠道停用但外部仍测试，可以按当前所属分组展示；前台统一文案，不加特殊标签，后台说明业务停用。
- 存档拓扑读取全部能力关系。停用渠道保留关系即可参与；启用渠道的独立禁用能力仍排除。删除渠道、模型撤下、移组、缺失关系及隐藏仍生效。独立PelicanGroups补足仅由停用渠道提供的已关联模型目录，不修改正式路由或旧自测模块。
- 本地service/router定向race通过：手动/自动停用及能力联动关闭后结果、详情和作品可读；后台channel_disabled=true且eligible=true；仅停用渠道的模型可选；移组后旧分组模型消失且原图直链404；隐藏仍有效，业务停用状态未改变。
- Default类型检查、修改文件ESLint、双前端构建、Go全仓构建、git diff --check通过。此轮未重新进行浏览器矩阵；之前预览进程可能仍是旧后端二进制，演示状态不能当作本轮新规则的运行证明。
- 两台服务器只读配置比对确认6项：ac→83、aishenji-958→71、dkl-016-ap-gpt-2hao→81、dkl-025→80、opentoken-2469→48、shuxin79→90。kedaya密钥对应9、shuai-gpt020密钥对应36但入口不同；同地址候选65/37密钥不同，排除误绑。详见渠道核对表。
- 密钥仅在远端进程内解密/读取并计算随机nonce摘要，摘要只在本地内存比对，原值和摘要不保存或输出。没有执行来源初始化/迁移，没有生产写入或付费模型调用。身份确认不等于生产已保存关联。

## 04:24 补充：关联核对与自动同步

以下为后续验证，下面02:50的样本统计保留为初次验收记录，不代表当前快照。

- 共用拓扑判定已接入公开筛选和管理报告；后台可查看已保存关系的分组、倍率及不展示原因。白名单读取渠道，不读取或返回密钥。新增SQLite回归覆盖多组共享、改名移组、能力禁用、渠道停用/恢复、模型撤下/恢复、单目标隐藏、解除关联、删除及来源移除；管理HTTP报告及凭据不泄露检查通过。
- 两套后台均实际操作取消关联并保存，报告立即变为未关联；重新选择渠道需重新选择有效模型。Classic隐藏单目标保存后，两组报告均显示测试展示已隐藏，恢复后重新有效。所有演示关系已恢复，8目标均关联有效、隐藏数0。
- 两套390px深浅色关联报告已实查，页面scrollWidth与clientWidth均390，无横向溢出。沿Default既有控件/Badge及Classic Semi Form/Tag，未引入UI库或改变用户认可的图库布局。
- `pull.py`有限两轮只读拉取均成功（每轮8目标/240条）；本地导入间隔设置1分钟，未点击立即同步。worker自动导入后，后台获取时间从04:15:15变为04:22:55，最近同步04:23:21。接口captured_at与私有快照逐字符串相等；Classic30秒轮询保留未保存隐藏草稿。有限轮运输已结束，没有留下无限运输任务或生产定时器。
- `go test -race -vet=off ./service ./router -run 'TestPelican' -count=1`、`go build ./...`、Python3项运输回归、Default类型检查及定向ESLint、双前端构建通过；ESLint剩1条既有Fast Refresh警告。后续仅文档补充和UI验收，未重跑无关测试。
- 真实生产已只读查渠道和分组，但准确绑定仍未完成：同地址多个渠道及停用候选见 `pelican-channel-audit-2026-09-23.md`。没有凭同名/同地址自动映射，没有读取或解密API密钥。本地页面继续明确标注演示分组关联。

仍未通过的发布门槛保留：实际渠道身份确认、受限生产运输、MySQL/PostgreSQL实机、最新生产基线复核、蓝绿候选与正式批准。Default新增关联文案补中英文，其他语言以及Classic完整翻译未完成。

## 来源与样本

通过只读SSH取得迁移后外部站点240条真实存档及8个目标。快照源捕获时间为2026-09-22T18:50:09.697020Z（北京时间9月23日02:50），不是持续实时数据。当前来源周期60分钟、标准答案21。本文不把之后的外部状态当作已核实。

原始记录分类：wrong149、error84、no_svg4、no_answer3。本站按原值保存，不重新评分。152条含SVG；最终安全检查147条允许预览，5条保留原文但不预览。无法预览不代表答案错误，也没有清理原证据。

本地验收使用同一批记录，但渠道ID、分组和倍率是明确标注的演示数据。不能据此声称已经绑定真实生产渠道。

## 已通过验证

| 范围 | 实际结果 |
| --- | --- |
| 真实导入 | 240条、重复导入0新增；check-only可在不监听端口时复验 |
| 存档修订 | 去重、来源变更拒绝、内容修订、旧内容恢复、容量拒绝整体回滚 |
| 配置 | 暂停拒绝导入、版本冲突、映射稳定及同渠道/模型重复关联拒绝 |
| 分组引用 | 同ID多组共享、渠道改名、移组、关闭、仅隐藏测试不关闭业务渠道 |
| 正式HTTP | 匿名拒绝；用户不可读管理接口；admin不可写；root可写；slave写拒绝；旧执行入口404 |
| 公开权限 | 私有分组及文案不泄露；隐藏详情/作品直链404；关闭历史时旧详情拒绝；管理员隐藏态仍可读 |
| SVG | 外链/脚本/事件/foreignObject/XML指令拒绝；普通本地use通过；自循环、互循环和指数扩张拒绝 |
| Default浏览器 | 真实三图上历史下；详情可点击；21标准答案、模型原答和真实计量；折叠草稿保持、整行排序、别名标题应用后前台更新 |
| Classic浏览器 | 真实作品与题目；21标准答案；自定义7分钟保存；隐藏并暂停后用户不可见、管理员仍可预览 |
| 主题布局 | 两套桌面浅色、390px手机深色实查；沿既有原生组件、语义色与主题 |

命令验证：

```sh
go test -race -vet=off ./pkg/pelicanarchive ./model ./service ./router -run 'Test(Pelican|CapabilityHTTP)' -count=1
go build ./...
# web/default
bun run typecheck
bun x eslint src/features/pelican-archive dev/pelican-preview.tsx
bun run build
# web/classic
bun run build
```

以上通过。ESLint有2条Fast Refresh非阻断警告，未隐藏。Python导出/拉取脚本语法检查和git diff --check通过。不是全仓默认行为回归，也没有生产或付费模型测试。

## 已发现并修复

- 本地会话缺少New-Api-User头，预览入口现沿项目鉴权要求设置。
- 本地内存库未初始化方言列名、默认连接寿命导致5分钟后丢表：改用正式InitDB连接初始化，关闭仅此预览内存池的连接寿命。
- 来源恢复旧内容时未激活旧修订：改为重新激活对应ID，其他修订留存不公开。
- 正常SVG本地use被过严检查误拦：允许有界无环引用，添加循环和扩张攻击回归。
- 其他配置保存导致展示文案草稿重置：以对应配置内容而非所有管理版本作为表单重建依据。
- Classic共享图片请求被某一组件取消：对独立AbortSignal请求禁用旧请求去重。

## 仍须完成的发布门槛

1. 8项目标身份与真实配置本地验收已完成；届时复核变化后经批准保存生产关联。当前无真实生产绑定，新增目标不按名称自动批准。
2. 专用只读运输账号、固定命令、目录权限、宿主定时器、网络和容器只读目录挂载；尚未在服务器写入。
3. 本机SQLite、MySQL8.2/8.4、PostgreSQL16合成数据库迁移及并发矩阵已通过；仍需首次生产初始化流程及候选验收，不能用合成测试替代。
4. 本轮GitHub/线上基线无变化；发布时再次复核并冻结源码和制品。补齐受控初始化工具，核对蓝绿配置、候选端口、备份和回滚，slave候选验收。正式程序使用二进制绑定挂载，须独立新制品路径。
5. 容量监控与证据归档保留策略：当前只有拒绝超限的门槛，没有自动清理；审计和索引空间另计。
6. 更广语言和键盘交互矩阵：Default本次中英文齐，其他四语言为英文回退；Classic新增文案以中文呈现，尚未补完整多语言翻译。

这些事项没有完成前不宣称正式生产可用。用户确认上线后才执行受审查的生产写入，继续沿既有蓝绿规范。
