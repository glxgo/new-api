# Claude 兼容入口

本仓库的活动项目规则已经迁移到 `AGENTS.md`，跨窗口代码状态位于 `CODEX_MEMORY.md`，运营状态位于 `../CODEX_MEMORY.md`。

- Codex 与其他代理都应以 `AGENTS.md` 为唯一活动规则正文。
- 迁移前的 Claude 规则原文保存在 `CLAUDE_LEGACY.md`，仅用于历史核对。
- 不要在本文件继续维护第二套规则。
